- 
  // taken from here https://tailwindui.com/components/application-ui/navigation/navbars
  var links = [
    {href: '#', label: 'Om oss', current: false}
  ]

header
  nav
    div(class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8")
      div(class="relative flex items-center justify-between h-16")
        div(class="absolute inset-y-0 right-0 flex items-center")
          //- <!-- Mobile menu button-->
          button(type="button" id="menu-btn" onclick="handleOnClick()" class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white" aria-controls="mobile-menu" aria-expanded="false")
            span(class="sr-only") Open main menu
            img(src='/img/menu.svg' id="menu-open" class="h-6 w-6" )
            img(src='/img/close.svg' id="menu-close" class="h-6 w-6" style="display: none")
        div(class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start")
          div(class="flex-shrink-0 flex items-center")
            a(href="/")
              img(
                class="block h-12 w-auto" 
                src="/img/robust-logo.svg"
                alt="Robust Tjänst av Netnod"
              )      
      div(id="mobile-menu" style="display: none")
        div(class="px-2 pt-2 pb-3 space-y-1")
          for item in links
                      a(
                        href=item.href
                        class=`px-3 py-2 ${item.current ? '' : 'text-gray-600 hover:bg-gray-100 hover:text-gray-900'}`
                        aria-current = item.current ? 'page' : undefined
                      )
                        = item.label

  //- search
  div(class="pt-8 flex flex-col")
    div(class="ml-4 md:ml-0 flex mb-8 justify-center")
      img(
        class=""
        width="380"
        src="/img/robust-logo.svg"
        alt="Robust Tjänst av Netnod"
      )

    div(
      class="px-4 my-12 flex flex-col items-center justify-center flex-grow"
    )
      form(method="post" action=namedPath('create_test') class="w-full md:w-3/4 lg:w-1/2 flex flex-row" ) 
        input(class="flex-grow p-4" autofocus type="text" name="url" placeholder="http://din.sida.se")
        input(type="submit" class="ml-2 w-24 btn btn-yellow" value="Testa")
      p(class="mt-4") Håller <em>din</em> sida måttet?


block script
  script(type="application/javascript").
    function handleOnClick() {
      const menu = document.getElementById("mobile-menu")
      const closeIcon = document.getElementById("menu-close")
      const menuIcon = document.getElementById("menu-open")

      if (menu.style.display === "block") {
        menu.style.display = "none"
        closeIcon.style.display = 'none'
        menuIcon.style.display = 'block'
      } else {
        menu.style.display = "block"; 
        closeIcon.style.display = 'block'
        menuIcon.style.display = 'none'
      }
    }